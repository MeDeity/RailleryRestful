spring:
  profiles:
    active: pro
  datasource:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/db_raillery
        username: root
        password: czcat
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true